{
  "id": "FEAT-002",
  "type": "feat",
  "description": "Exercise the generator end-to-end to produce demonstration output and prove the pipeline works. Install Argos Translate + the en->pt model, run a real (sampled) fetch+translate pass, and regenerate at least data/dictionaries/pt-br/skills.json with real GW2 skill data translated to PT-BR. Do NOT run the full ~90k items dataset; a representative sample is sufficient. This validates the API layer, translation layer, caching/resumability, and output-schema compatibility against the actual C++ loader format. Depends on FEAT-001.",
  "status": "completed",
  "steps": [
    "Create and use a virtualenv: python3 -m venv .venv && source .venv/bin/activate, then pip install -r tools/dictionary-generator/requirements.txt. If the full Argos install or heavy transitive deps (ctranslate2/torch) fail or time out in the sandbox, record that in findings and fall back to producing a demonstration sample using --no-translate (real EN data, structure-correct JSON) so the pipeline is still proven; do NOT mark blocked solely because model download is slow.",
    "Install the en->pt model: python3 tools/dictionary-generator/generate_dictionaries.py --install-model (or the equivalent action). Confirm argostranslate.translate can translate a sample string EN->PT.",
    "Run a sampled skills generation, e.g.: python3 tools/dictionary-generator/generate_dictionaries.py --category skills --limit 300 (choose a limit that completes in a reasonable time; the goal is a real, non-trivial sample, ideally the full ~few-thousand skills if time permits). Confirm the raw cache under tools/dictionary-generator/cache/ is populated and that a re-run is fast (cache hit).",
    "Verify the produced data/dictionaries/pt-br/skills.json parses with json.load, has top-level locale=pt-br, category=skills, version=1.0.0, and a non-trivial entries array where every entry has non-empty string 'en' and 'translated'. Confirm the pre-existing curated example entries are not required (the generator output is the demonstration) and note in findings how many entries were produced.",
    "Demonstrate resumability: interrupt-free re-run of the same command should reuse cache and not re-fetch/re-translate (verify via the summary counts / cache-hit reporting).",
    "Record in the feature findings: which categories were run, sample sizes, whether Argos model installed successfully, runtime observed, and any fallbacks used."
  ],
  "acceptance_criteria": [
    "data/dictionaries/pt-br/skills.json is regenerated by the tool and contains a non-trivial number of real GW2 skill entries (substantially more than the original 15 examples), each with 'en' and 'translated' string fields.",
    "The generated skills.json parses via json.load and matches the schema in src/translation/dictionary.cpp (locale/category/version/entries with en+translated).",
    "The tools/dictionary-generator/cache/ directory is populated with raw EN data and (if translation ran) a translation cache, proving idempotent/resumable behavior.",
    "A second run of the same command completes quickly using the cache (documented in findings).",
    "If Argos could not be fully installed/run in the sandbox, findings clearly state the fallback used and confirm the pipeline is otherwise functional."
  ],
  "verification": [
    "Run: python3 -c \"import json,sys; d=json.load(open('data/dictionaries/pt-br/skills.json')); assert d['locale']=='pt-br' and d['category']=='skills'; e=d['entries']; assert isinstance(e,list) and len(e)>15; assert all(isinstance(x['en'],str) and x['en'] and isinstance(x['translated'],str) and x['translated'] for x in e); print('OK', len(e), 'entries')\"",
    "Run: ls -la tools/dictionary-generator/cache/ and confirm raw cache (and translations.json if translation ran) exist",
    "Re-run the sampled skills command and confirm it reports cache hits / completes quickly"
  ],
  "blocked_reason": null,
  "findings": "COMPLETED end-to-end with real Argos offline translation (Opcao C). No fallback to --no-translate was needed; real GW2 data was fetched AND translated EN->PT.\n\nCategories run: skills (full dataset).\nSample sizes: initial gauge run --limit 300 (260 unique names); then FULL skills run with no limit: 4702 skill IDs fetched from api.guildwars2.com/v2/skills in 24 batches of <=200, yielding 2963 unique English names (dedup by normalized/lowercase key), all translated to PT-BR. Final data/dictionaries/pt-br/skills.json has 2963 entries (vs the original 15 curated examples).\n\nArgos install: SUCCEEDED. IMPORTANT setup nuance for the maintainer: a plain 'pip install -r requirements.txt' FAILS on Python 3.9 because argostranslate pulls the latest spacy (3.8.16), whose build-time dep thinc>=8.3.12 requires Python >=3.10 and has no 3.9 wheel (build-from-source error). Workaround that WORKED on Py3.9: pre-install a 3.9-compatible spacy first, e.g. 'pip install \"spacy<3.8\"' then 'pip install -r requirements.txt' (resolves to spacy 3.7.5, thinc 8.2.5, ctranslate2 4.8.1, sentencepiece 0.2.2, stanza 1.10.1, argostranslate 1.11.0). On Python 3.10+ the plain requirements install should work directly. requirements.txt was NOT modified (its pins are still valid for 3.10+); this is only a Py3.9 environment note.\n\nModel install: 'generate_dictionaries.py --install-model' downloaded/installed the en->pt package in ~4s. Verified a live EN->PT sample: 'Fireball'->'Bola de Fogo', 'Meteor Shower'->'Chuveiro de meteoros', 'Healing Spring'->'Primavera de Cura', 'Greatsword'->'Grande espada'.\n\nRuntime observed: gauge run (300 IDs / 260 translations) ~11s. Full skills run (4702 IDs, 2703 new translations + 260 cache hits) ~86s wall (multi-core; ~27 translations/sec after model warmup). Cold model load adds ~2-3s on first translate.\n\nResumability PROVEN: an immediate re-run of the same full command made 0 HTTP requests (25 API cache hits), 0 translations (2963 translation cache hits), and finished in 0.17s. Cache lives under tools/dictionary-generator/cache/ (gitignored): raw/skills/ holds 24 batch files + _ids.json; translations.json (~116KB) holds the EN->PT map.\n\nSchema check: generated JSON is byte-compatible with the C++ loader src/translation/dictionary.cpp (reads entries[], each with en+translated, normalizes en to lowercase for case-insensitive lookup). Output is UTF-8, ensure_ascii=false, 4-space indent, locale=pt-br, category=skills, version=1.0.0. Verification one-liner passed: 'OK 2963 entries'.\n\nGit hygiene: added .venv/, tools/dictionary-generator/.venv/, __pycache__/ and *.pyc to .gitignore. cache/ was already gitignored. Committed the regenerated skills.json + .gitignore locally; did NOT commit venv or cache; did NOT push or open a PR. Did NOT modify any C++ code. Did NOT use Google Translate or SQLite (steering confirmed Opcao C).\n\nMaintainer note: full items (~90k IDs) was intentionally NOT run (long-running by design). The pipeline is proven and ready: 'generate_dictionaries.py --category all' or '--category items' will regenerate the remaining datasets, resuming from cache."
}
